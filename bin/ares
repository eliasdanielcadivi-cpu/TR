#!/bin/bash
# Generado por 'ini' v2.1 - Ecosistema TRON (Local)
# Proyecto: ares

# Determinar raíz relativa a la ubicación del script
export TR_PROJECT_ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"

exec uv run --project "$TR_PROJECT_ROOT" python "$TR_PROJECT_ROOT/src/main.py" "$@"
